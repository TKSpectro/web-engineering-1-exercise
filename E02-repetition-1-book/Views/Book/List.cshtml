@model IEnumerable<Book>

<h2>@ViewBag.Title</h2>

<table class="table">
    <tr>
        <th>Name</th>
        <th></th>
    </tr>

@foreach (var book in Model)
{
    <tr>
        <td>@book.Name</td>
        <td>@Html.ActionLink("Details", "Details", new { id = book.Id })</td>
    </tr>
}
</table>